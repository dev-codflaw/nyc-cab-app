<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CabConnect - Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            width: 100%;
            max-width: 380px;
            margin: 0 auto;
        }

        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 40px 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
        }

        .logo {
            font-size: 32px;
            margin-bottom: 8px;
        }

        .title {
            color: #333;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .subtitle {
            color: #666;
            font-size: 16px;
        }

        .login-options {
            display: flex;
            flex-direction: column;
            gap: 16px;
            margin-bottom: 32px;
        }

        .login-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            padding: 16px 20px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            background: #fff;
            color: #333;
            text-decoration: none;
            font-weight: 500;
            font-size: 16px;
            transition: all 0.2s ease;
            min-height: 54px;
            -webkit-tap-highlight-color: transparent;
        }

        .login-btn:active {
            transform: scale(0.98);
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .mobile-btn {
            border-color: #667eea;
            background: rgba(102, 126, 234, 0.05);
        }

        .email-btn {
            border-color: #28a745;
            background: rgba(40, 167, 69, 0.05);
        }

        .gmail-btn {
            border-color: #dc3545;
            background: rgba(220, 53, 69, 0.05);
        }

        .apple-btn {
            border-color: #333;
            background: rgba(51, 51, 51, 0.05);
        }

        .icon {
            font-size: 20px;
        }

        .back-link {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            padding: 12px;
            border-radius: 10px;
            transition: all 0.2s ease;
            -webkit-tap-highlight-color: transparent;
        }

        .back-link:active {
            background: rgba(102, 126, 234, 0.1);
            transform: scale(0.98);
        }

        @media (max-width: 480px) {
            body {
                padding: 16px;
            }
            
            .card {
                padding: 32px 24px;
            }
            
            .title {
                font-size: 22px;
            }
            
            .login-btn {
                padding: 14px 18px;
                font-size: 15px;
            }
        }

        @media (max-width: 360px) {
            .card {
                padding: 28px 20px;
            }
            
            .login-btn {
                padding: 12px 16px;
                min-height: 50px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="header">
                <div class="logo">🚖</div>
                <h1 class="title">Welcome Back</h1>
                <p class="subtitle">Choose your login method</p>
            </div>

            <div class="login-options">
                <a href="/auth/mobile" class="login-btn mobile-btn">
                    <span class="icon">📱</span>
                    <span>Login with Mobile</span>
                </a>

                <a href="/auth/email" class="login-btn email-btn">
                    <span class="icon">✉️</span>
                    <span>Login with Email</span>
                </a>

                {# <a href="/auth/gmail" class="login-btn gmail-btn">
                    <span class="icon">🔴</span>
                    <span>Login with Gmail</span>
                </a>

                <a href="/auth/apple" class="login-btn apple-btn">
                    <span class="icon">🍎</span>
                    <span>Login with Apple</span>
                </a> #}
            </div>

            <a href="/" class="back-link">
                <span>←</span>
                <span>Back to Home</span>
            </a>
        </div>
    </div>
</body>
</html>