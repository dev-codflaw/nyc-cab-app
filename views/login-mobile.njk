<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CabConnect - Mobile Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
            padding: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            width: 100%;
            max-width: 380px;
            margin: 0 auto;
        }

        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 40px 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .header {
            text-align: center;
            margin-bottom: 32px;
        }

        .logo {
            font-size: 32px;
            margin-bottom: 8px;
        }

        .title {
            color: #333;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .subtitle {
            color: #666;
            font-size: 16px;
            line-height: 1.4;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .label {
            display: block;
            color: #333;
            font-weight: 500;
            margin-bottom: 8px;
            font-size: 15px;
        }

        .phone-input-group {
            display: flex;
            gap: 12px;
            margin-bottom: 24px;
        }

        .country-select {
            flex: 0 0 100px;
            padding: 16px 12px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            background: #fff;
            color: #333;
            font-weight: 500;
            -webkit-appearance: none;
            appearance: none;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
            background-position: right 12px center;
            background-repeat: no-repeat;
            background-size: 16px;
        }

        .phone-input {
            flex: 1;
            padding: 16px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            background: #fff;
            transition: all 0.2s;
        }

        .phone-input:focus,
        .country-select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .otp-input-group {
            display: flex;
            gap: 12px;
            justify-content: center;
            margin: 24px 0;
        }

        .otp-digit {
            width: 50px;
            height: 50px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            text-align: center;
            font-size: 20px;
            font-weight: 600;
            background: #fff;
            transition: all 0.2s;
        }

        .otp-digit:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .btn {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            margin-bottom: 16px;
            min-height: 54px;
            -webkit-tap-highlight-color: transparent;
        }

        .btn:active {
            transform: scale(0.98);
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
        }

        .btn:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .resend-link {
            display: block;
            text-align: center;
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            padding: 12px;
            border-radius: 8px;
            margin-bottom: 20px;
            -webkit-tap-highlight-color: transparent;
        }

        .resend-link:active {
            background: rgba(102, 126, 234, 0.1);
        }

        .timer {
            text-align: center;
            color: #666;
            font-size: 14px;
            margin-bottom: 16px;
        }

        .back-link {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            color: #667eea;
            text-decoration: none;
            font-weight: 500;
            padding: 12px;
            border-radius: 10px;
            transition: all 0.2s ease;
            -webkit-tap-highlight-color: transparent;
        }

        .back-link:active {
            background: rgba(102, 126, 234, 0.1);
            transform: scale(0.98);
        }

        .screen {
            display: none;
        }

        .screen.active {
            display: block;
        }

        @media (max-width: 480px) {
            body {
                padding: 16px;
            }
            
            .card {
                padding: 32px 24px;
            }
            
            .otp-digit {
                width: 45px;
                height: 45px;
                font-size: 18px;
            }
            
            .otp-input-group {
                gap: 8px;
            }
        }

        @media (max-width: 360px) {
            .card {
                padding: 28px 20px;
            }
            
            .phone-input-group {
                gap: 8px;
            }
            
            .country-select {
                flex: 0 0 85px;
                padding: 16px 8px;
            }
            
            .otp-digit {
                width: 40px;
                height: 40px;
                font-size: 16px;
            }
            
            .otp-input-group {
                gap: 6px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Mobile Number Screen -->
        <div id="mobileScreen" class="screen active">
            <div class="card">
                <div class="header">
                    <div class="logo">📱</div>
                    <h1 class="title">Enter Mobile Number</h1>
                    <p class="subtitle">We'll send you a verification code</p>
                </div>

                <form id="mobileForm" method="POST" action="/auth/send-otp">
                    <div class="form-group">
                        <label class="label">Mobile Number</label>
                        <div class="phone-input-group">
                            <select class="country-select" id="countryCode">
                                <option value="+91">🇮🇳 +91</option>
                                <option value="+1">🇺🇸 +1</option>

                            </select>
                            <input 
                                type="tel" 
                                class="phone-input" 
                                placeholder="Enter mobile number"
                                id="mobileNumber"
                                maxlength="10"
                                required
                            >
                        </div>
                    </div>

                    <!-- Hidden field to hold final mobile parameter -->
                    <input type="hidden" name="mobile" id="fullMobile">
                    <button type="submit" class="btn">Send OTP</button>
                </form>

                <a href="/auth/login-methods" class="back-link">
                    <span>←</span>
                    <span>Back to Login Options</span>
                </a>
            <a href="/" class="back-link">
                <span>←</span>
                <span>Back to Home</span>
            </a>
            </div>
        </div>

    </div>

  <script>
  document.getElementById('mobileForm').addEventListener('submit', function(event) {
    const countryCode = document.getElementById('countryCode').value;
    const number = document.getElementById('mobileNumber').value.trim();
    
    // Combine country code and number
    const fullMobile = countryCode + number;
    
    // Set the hidden input value
    document.getElementById('fullMobile').value = fullMobile;
  });
</script>  
</body>
</html>